# Copy this to .env.local for local development (already defaults to localhost)
# For Netlify: set VITE_API_URL in Site Settings → Environment Variables

# Your Render backend URL (set this in Netlify dashboard, NOT here)
VITE_API_URL=https://your-render-service.onrender.com
